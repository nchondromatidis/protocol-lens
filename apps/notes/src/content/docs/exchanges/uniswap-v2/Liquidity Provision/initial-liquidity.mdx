---
title: Initial Liquidity
description: Uniswap V2 workflows
sidebar:
  order: 3000
---
import { DesmosChart } from '@defi-notes/workflows/components/DesmosChart';
import { ProtocolWorkflowTrace } from '@defi-notes/workflows/components/ProtocolWorkflowTrace';
import { extractProtocolWorkflowCode } from "@defi-notes/workflows/protocols/extract-code/protocol-workflow";
import { extractProtocolCodeSlice } from "@defi-notes/workflows/protocols/extract-code/protocol-sources";
import { Code } from '@astrojs/starlight/components';
import { uniswapV2ChartId } from '@constants/charts';

This page covers the necessary concepts for the reader to understand the **initial liquidity flow in Uniswap V2**.

## Concepts
The theory behind a Uniswap pool, also known as a pair, is rooted in the concept of an **Automated Market Maker (AMM)**.

### Automated Market Makers
Automated Market Maker (AMM) is the underlying architecture behind Uniswap V2
- Alternative to ordering books, economically inefficient on-chain due to the **high gas costs**
- Prices are determined by a **mathematical formulae** written into protocol code
- Enable **permissionless**, **middleman-free** trading
- Provide **instant** execution on rates and slippage

### Constant Product Market Maker
The specific type of AMM used by Uniswap v2 is the **Constant Product Market Maker** (CPMM), which is governed by the formula

<div className="text-center">$x \cdot y = k$ </div>

- $x$ and $y$ represent the reserve balances of the two tokens in the pool
- $k$ is the **invariant**, stays constant
- Trading curve: The protocol requires that $k$ remains **constant during a trade** (ignoring fees)
  - The **spot price** is determined by the ratio of the tokens in the pool $p = y/x$
  - When a trader buys/withdraws token $x$, they must deposit a proportional amount of token $y$ to **ensure the product $x \cdot y$ remains equal to $k$**.
- Enforces a natural inverse relationship between the two assets, ensuring that as one gets scarcer, its price rises.
- You cannot deplete the reserve of any asset (asymptotic to zero).


### Liquidity Providers (LPs)
Liquidity Providers are users or smart contracts that deposit an **equivalent value** of **two ERC-20 tokens** into a **specific pair contract**.
In exchange, they get **LP tokens** that represent their **deposit share in the pool**.

- First Liquidity Provider:
The individual who performs the initial deposit into a new pool. They are responsible for seeding the reserves and setting the initial exchange rate (price) of the pair.
- Subsequent Providers:
LPs who add to an existing pool are incentivized to deposit tokens proportional to the current reserve ratio.

Depositing at an incorrect ratio creates an immediate arbitrage opportunity, which external parties will likely exploit.

### Initial Reserves
When setting up the pool, the initial amounts of tokenA and tokenN can be in any proportion, since no prior balances exist for reference.

<Code
  code={extractProtocolCodeSlice([
    ['contracts/uniswap-v2/v2-periphery/contracts/UniswapV2Router02.sol:UniswapV2Router02.sol', 46, 48]
  ])}
  lang="solidity"
/>

### Mint
LP tokens are ERC20 tokens that are minted to the LP.

Let's analyze the formula that calculates LP tokens to the first LP.

<Code
  code={extractProtocolCodeSlice([
    ['contracts/uniswap-v2/v2-core/contracts/UniswapV2Pair.sol:UniswapV2Pair', 119, 122]
  ])}
  lang="solidity"
/>

The initial liquidity calculation contains two important elements
- `Math.sqrt(amount0.mul(amount1))` $\Rightarrow$ $\sqrt{x \cdot y}$
- `.sub(MINIMUM_LIQUIDITY);` $\Rightarrow$ $- 1000$

Let's try to understand each part

##### Why square root of the token amount product
Let's **try** to go into the mind of the creator and reach to the same formula.

Our goal is to find a number that correctly measures the LP's token pair ownership in the pool.

---

1 . Logic steps
- Principle: The LP token number must represent the ownership the LP has on the pool assets.
  - LP tokens should not change in quantity simply because prices (aka ratio $\frac{x}{y}$) change due to trades.
  - Ownership should not change unless new liquidity is added or removed.
- Definition: $k$ is constant along the curve ($x \cdot y = k$)
- Let’s find some functions that respect the above:
  - $x \cdot y$ : stays constant by definition
  - $\sqrt{x \cdot y}$ : stays constant (property of the parabola $x \cdot y = k$)
  - $\frac{x + y}{2}$ : does NOT stay constant (arithmetic mean)
  - $x + y$ : does NOT stay constant

| Function    | **Ratio 1**<br/>(10 ETH, 10 USDC)<br/>k=100 | **Ratio 2**<br/>(100 ETH, 1 USDC)<br/>k=100 | **P1 Constant?** |
|:------------|:--------------------------------------------|:--------------------------------------------|:-----------------|
| **x·y**     | 100                                         | 100                                         | ✅                |
| **√(x·y)**  | 10                                          | 10                                          | ✅                |
| **x+y**     | 20                                          | 101                                         | ❌                |
| **(x+y)/2** | 10                                          | 50.5                                        | ❌                |

---

2 . Logic steps:
- Principle: The LP token number should be able to scale proportional to the reserves of the pool.
  - eg, LP token calculation must double/half when the reserves of the pool double/halve.
  - So the function L that calculates the LP tokens should respect both
    - $L(nx,ny) = n*L(x,y)$
    - $L(\frac{x}{n},\frac{y}{n}) = \frac{L(x,y)}{n}$

---

3 . Solution (1,2):
- We could try to solve 2 using math, but we are already limited by 1
  - We try both functions from 1
    - $\sqrt{x \cdot y}$ : scales quadratically with the value of the pool
    - $x \cdot y$ : scales linearly with the value of the pool


| Function   | **k=100**<br/>(10 ETH,10 USDC) | **k=100**<br/>(100 ETH,1 USDC) | **k=400**<br/>(20 ETH,20 USDC) | **k=400**<br/>(200 ETH,2 USDC) | **P1 Constant?** | **P2 2x Scale?** |
|:-----------|:-------------------------------|:-------------------------------|:-------------------------------|:-------------------------------|:-----------------|:-----------------|
| **x·y**    | 100                            | 100                            | 400                            | 400                            | ✅                | ❌ (4x)           |
| **√(x·y)** | 10                             | 10                             | 20                             | 20                             | ✅                | ✅ (2x)           |


##### Why burn one thousand LP tokens
Summary: it makes an initial inflation attack for initial liquidity very costly

You can read more about it: <a href="/fundamentals/defi/vaults#defence-3-extemelly-lossy-attack">Burn Initial Shares</a>

### Price Oracle
Summary: it makes oracle price resistant to manipulation

You can read about it: <a href="/exchanges/uniswap-v2/price-oracle/">Price Oracle</a>

## Ecosystem
| pair@26/02/2026 | x             | y                | k               | uniswap                                                                                           | etherscan                                                                                        |
|-----------------|---------------|------------------|-----------------|---------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------|
| USDC/WETH       | 9.2M * $10^6$ | 4.4K * $10^{18}$ | 4.076 $10^{34}$ | [link](https://app.uniswap.org/explore/pools/ethereum/0xB4e16d0168e52d35CaCD2c6185b44281Ec28C9Dc) | [link](https://etherscan.io/address/0xB4e16d0168e52d35CaCD2c6185b44281Ec28C9Dc#asset-multichain) |

## Chart
With the interactive chart below you can experiment with all the concepts above to deepen your understanding.

Each folder in the chart represents a concept that you can show or hide.

<DesmosChart graphId={uniswapV2ChartId} height="500px" client:load />

## Function Trace
In this example we will set the initial erc20 token deposits.

This is the code that runs to produce the function trace

<Code
  code={extractProtocolWorkflowCode([
    ['UniswapV2Workflows', 'initialLiquidity'],
    ['UniswapV2Workflows', '_addLiquidity']
  ])}
  lang="ts"
/>

<ProtocolWorkflowTrace
  protocol="uniswapV2"
  workflow="initialLiquidity"
  header='Initial Liquidity'
  warnMessages={['UniswapV2Pair uses solidity v0.5.16', 'Function traces are not supported for v0.5.16']}
  client:load
/>